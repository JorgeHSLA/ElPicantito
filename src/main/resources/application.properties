# Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/picantitodb}
spring.datasource.username=${DB_USERNAME:taquito}
spring.datasource.password=${DB_PASSWORD:taquito123}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=${JWT_SECRET:TacosPicantitosSuperSecretKeyForJWT2024MustBeAtLeast256BitsLong!}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:elpicantitotacosautenticos@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ailkspvblmlheaya}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Server Configuration
server.port=9998

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.picantito=DEBUG
